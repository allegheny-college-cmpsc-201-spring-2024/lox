setup: |
  echo "🧹\tClean from previous compilation (mvn clean)"
  mvn clean -q || true
  echo "🛠️\tCompiling program (mvn compile)"
  mvn compile -q || true
---
- description: Program produces output consistent with allowed multiple inheritors
  category: Tests
  check: ExecuteCommand
  options:
    command: mvn test -Dtest=BaseTests#testMultipleInherit -f "interpreter/pom.xml"
- docs/reflection.md:
  - description: Reflection contains no TODOs
    category: TODOs
    check: MatchFileFragment
    options:
      fragment: TODO
      count: 0
      exact: true
